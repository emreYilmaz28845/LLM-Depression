# Gemma 4 DAIC dedicated MN5 environment (offline install only)
#
# Install exclusively from a locally built wheelhouse:
#   python -m pip install --no-index --find-links <wheelhouse> -r requirements_mn5_gemma4.txt
#
# MN5 has no outbound internet. Never run pip/conda network installation,
# huggingface-cli download, snapshot_download, git clone/pull, wget, or curl
# on MN5. Model and processor load from the GPFS snapshot with
# local_files_only=True and HF_HUB_OFFLINE=1 / TRANSFORMERS_OFFLINE=1 /
# HF_DATASETS_OFFLINE=1.

# Core pins (runbook §12)
torch==2.7.1+cu126
torchvision==0.22.1+cu126
transformers==5.14.1
peft==0.19.1
accelerate==1.14.0
numpy==1.26.4
librosa==0.11.0
soundfile==0.13.1

# Gemma4UnifiedProcessor requires the Transformers vision backend (Pillow);
# torchvision is pinned above for the same backend.
Pillow==11.3.0

# Runtime dependencies of the repository path (pinned for reproducibility)
safetensors==0.5.3
tokenizers==0.21.1
huggingface-hub==0.30.2
datasets==3.6.1
pyyaml==6.0.2
tqdm==4.67.1
requests==2.32.4
packaging==24.2
filelock==3.17.0
fsspec==2025.6.1
typing-extensions==4.13.2
jinja2==3.1.6
markupsafe==3.0.2
regex==2024.11.6
scipy==1.15.2
numba==0.61.2
audioread==3.0.1
decorator==5.2.1
lazy_loader==0.4
msgpack==1.1.1
soxr==0.5.0.post1
psutil==7.0.0
