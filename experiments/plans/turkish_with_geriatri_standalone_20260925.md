# Turkish vs TurkishWithGeriatri: standalone execution plan

This plan is the handoff for continuing PR #266 on branch `agent/exp-turkish-geriatri` in `/home/emre/Projects/AudioLLM/worktrees/LLM-Depression-exp-turkish-geriatri`. Keep the PR open. Commit and push further in-scope work to this branch; the PR updates automatically. Do not merge it without the user's explicit task-specific authorization. Do not force-push. Before each edit, commit, deployment, or submission, run `python tools/check_worktree_pin.py`. Preserve unrelated work and never stage `AGENTS.md`, `.agents/`, `docs/`, or `skills-lock.json`.

## Objective and fixed scope

Rerun the Turkish standalone comparison for three model/modality recipes, each in two data arms, five folds (`0..4`), seed `1337`:

| Model | Modality | Turkish baseline | TurkishWithGeriatri |
| --- | --- | --- | --- |
| Qwen3.8-27B | text-only | `turkish_pooled_t17` | four-source combined Turkish |
| Qwen3-Omni Thinker | audio-only | `turkish_pooled_t17` | four-source combined Turkish |
| Qwen3-Omni Thinker | audio+text | `turkish_pooled_t17` | four-source combined Turkish |

This means 30 training folds and 30 standalone fold evaluations if all jobs succeed. Do not add models, modalities, thresholds, seeds, or other datasets. The existing baseline definitions are `experiments/definitions/qwen38-standalone-promptcontext-20260922.yaml` and `experiments/definitions/qwen3omni-standalone-promptcontext-20260923.yaml`; use their Turkish entries as recipe authorities. Baseline is **positive plus negative question sets**, not positive-only. Both arms use candidate-label likelihood and macro-F1 checkpoint selection. The Turkish `train_val` protocol reports selected validation, **not an independent held-out test**; say this in every report.

## Data contract

The four inputs are `Turkish_Positive_Only`, `Turkish_Negative_Only`, `Turkish_Geriatri/positive`, and `Turkish_Geriatri/negative`. Original Turkish has 120 people, geriatri has 102 *different* people. Reused patient codes across cohorts never identify the same person; retain geriatri-specific namespacing. Geriatri label source is the CSV `depresyon_skoru` with cutoff 17, not the workbook or BAÖ. One workbook BDÖ value disagrees with CSV; user explicitly chose CSV value 14. Never publish subject IDs or transcripts.

PR #266 already contains a local four-source manifest: 4,330 rows, 222 people, 112 positive and 110 negative, with 1,051/1,170/919/1,190 rows by source. Manifest content hash in split metadata is `570a9b7a4b6a1b1bf3c5172d3e07ec94be2000dc090e5d796ccdb592ebcc6960`. Local geriatri Qwen3-ASR files are `whisper_transcripts_qwen3_asr_positive.jsonl` (919 complete) and `whisper_transcripts_qwen3_asr_negative.jsonl` (1,190 complete), both under `/media/emre/Backup/AudioLLM/Datasets/Turkish_Geriatri/`. The local manifest and split artifacts are ignored outputs, not Git inputs; confirm them on disk. The geriatri directory was absent on MN5 at handoff. Any cluster transfer of data, transcripts, manifest, or splits must be explicit, new-target-safe, verified, and follow `docs/DEVICES.md`, `docs/MN5_AGENT_EXECUTION_RUNBOOK.md`, and the `mn5-cluster-ops` skill. Do not use `--delete` or overwrite remote files without separate approval.

## Mandatory preflight: make the comparison valid

1. Read `AGENTS.md`, the four required repository docs, relevant skills, both baseline definitions, their Turkish configs, PR #266 diff, and the managed-worktree pin. Check branch, PR state, source SHA, clean worktree, and local input files. Do not treat this plan as a substitute for current files.
2. Preserve the original 120 people's exact baseline fold assignments. The PR #266 combined builder currently re-stratifies all 222 people; its existing split file is **not accepted** for a matched comparison. Obtain or reproducibly build the baseline pooled split mapping, assign the 102 new geriatri people deterministically into five folds, and regenerate combined splits. Assert every original subject has the same fold ID in both arms, every combined subject is held out exactly once, and no subject crosses train/validation within a fold. Store split hashes and mapping-audit evidence without disclosing IDs in public artifacts.
3. Align treatment configs to each baseline config on model identity/revision, prompt text and `promptcontext_v1`, candidate labels, likelihood view, training hyperparameters, LoRA target policy, checkpoint selection, windowing, effective global batch, and evaluation aggregation. The PR #266 treatment configs copied older positive-only recipes and **must not be submitted as-is**. In particular, the baseline pooled text recipe makes a positive/negative question pair per subject and uses `turkish_pooled_text_pair_mean_margin_strict_v1`; the treatment currently makes one combined text example per subject. Implement a four-source paired-text equivalent and audit all `dataset_variant == "pooled_t17"` special cases in `src/data/runtime.py`, `src/evaluate.py`, `src/aggregate.py`, and related code. Keep question-condition breakdown and subject-level aggregation equivalent. The Qwen3-Omni baseline uses two nodes x four H100 with accumulation 16; treatment currently differs. Do not change one arm only.
4. Add targeted tests for four-source membership, cohort namespacing, CSV label precedence, fold lock, text question pairing, evaluation aggregation, and identical shared recipe fields across arms. Verify all six configs resolve to the intended model and modality. Run local validation skill checks and model-free sanity checks before training. No production job until these gates pass.
5. Register one explicit experiment group/definition with all six cells, five folds, seed, hypothesis, comparison population, evaluation view, and run naming. Use the `experiment-tracking` skill and current schemas/CLI. Commit source/config/tests/definition to this same branch, inspect staged names for private paths, push to PR #266, and deploy only a clean committed SHA. If source changes later, create a new immutable deployment and new attempt identities; never silently continue old jobs on altered source.

## Execution on MN5

Use managed `tools/exp.py` deployment and submission, not the legacy mutable checkout. Before transfer/submit read the MN5 skill and runbook. Verify the remote data, ASR JSONL, manifest, split mapping, and hashes; the manifest and context are ignored local outputs and require explicit transfer. Choose unique run and attempt names for each cell/fold. Run a small smoke first for each distinct model path (Qwen3.8 text, Qwen3-Omni audio, Qwen3-Omni audio+text), checking dataset counts, question pairing, resolved config, GPU shape, checkpoint location, and likelihood evaluation. Then submit all planned folds through Slurm with the baseline resource shape. Monitor every train and standalone evaluation to completion; submission is not completion. Record failed jobs and bounded retries as new attempts, following the runbook. Do not run training on login or transfer nodes, change shared environments, overwrite evidence, or expand the matrix silently.

Sync compact evidence back locally: `run_config.yaml`, tracking sidecars, metrics, subject-level predictions, final summaries, logs, and audits. Checkpoint sync follows the repository storage convention. Validate each fold locally and rebuild/query the experiment registry. Report only locally verifiable results with run/attempt/fold IDs, source/config/manifest/split hashes, selected checkpoint, likelihood view/backend, aggregation, and evidence paths. Use `provenance-reporting` before writing any metric. Update PR #266 description with exact executed attempts and caveats; leave the PR open for user review.

## Comparison and acceptance

**Never remove geriatri from the standard evaluation.** The TurkishWithGeriatri arm trains and runs its normal five-fold evaluation on **all 222 people**, including held-out geriatri people in each fold. Its default/headline dataset result is the full-222 result. Keep that evaluation and its complete subject-level predictions intact. Do not configure a 120-person evaluation in its place.

Separately, the primary *cross-arm comparison* asks whether adding geriatri to training changes performance on the **same original 120 people**. Derive the treatment's original-120 subset from the already completed full-222 subject-level predictions, using exactly the baseline evaluation semantics and aligned folds; do not retrain or replace the standard evaluation for this subset. Compare paired fold-level Macro-F1, positive-F1, and UAR against the freshly rerun baseline. Confirm the subset operation does not alter per-subject predictions or the declared aggregation. Report fold values and the five-fold mean; do not mix a pooled subject metric with a fold mean. Report the full-222 treatment result and geriatri-only subgroup separately, with their evaluation populations clearly named. A direct baseline-120 versus treatment-222 score difference does **not** isolate the effect of adding training data. Do not claim statistical significance unless a test and multiplicity policy were prespecified and validated.

Use the `experimental-results-tables` skill for the reader-facing comparison table. Each row must state model, modality, data arm, evaluation population, view/backend, aggregation, and provenance; show Macro-F1, positive-F1, and UAR. No blank or failed fold becomes zero. The task is complete only after all six cells have five validated folds and evaluations, the matched original-120 comparison is reproducible from local artifacts, secondary populations are labeled, failures/retries are recorded, and the PR contains code/config/definition plus an evidence-backed summary. If any prerequisite fails, stop at that gate and report the exact blocker; do not substitute a convenient recipe or population.
